# Backend URL for the unlisted /lab/relationship-magazine tool (see
# ../wa-magazine-service/). Leave unset locally if you're not working on
# that page — it renders a small "not configured" notice instead of crashing.
VITE_WA_MAGAZINE_API_URL=
